services:
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
      args:
        VITE_API_URL: "http://100.76.65.1:8111"
    ports:
      - "8112:80"
    volumes:
      - ./assets/models:/usr/share/nginx/html/assets/models:ro
